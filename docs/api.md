# 接口文档

> 版本：v0.1 · Base URL：`https://api.example.com` · 更新日期：YYYY-MM-DD

## 1. 通用约定

### 1.1 认证与鉴权

（Bearer Token / Cookie / API Key 等。）

### 1.2 请求格式

- `Content-Type`：`application/json`
- 字符编码：UTF-8

### 1.3 响应体约定

**成功示例**：

```json
{
  "code": 0,
  "data": {},
  "message": "ok"
}
```

**错误示例**：

```json
{
  "code": 40001,
  "message": "错误说明",
  "details": {}
}
```

### 1.4 错误码

| code | HTTP | 说明 |
|------|------|------|
| 0 | 200 | 成功 |
| | | |

### 1.5 分页（如适用）

| 参数 | 类型 | 说明 |
|------|------|------|
| page | int | 页码，从 1 开始 |
| page_size | int | 每页条数 |

## 2. 接口设计问题与决策

（记录评审中的争议与结论，便于迭代时追溯。）

| 议题 | 方案 | 理由 | 日期 |
|------|------|------|------|
| 例：列表是否游标分页 | | | |

## 3. 接口清单

### 3.1 模块：{模块名}

#### `GET /v1/...`

- **说明**：
- **权限**：
- **Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|

- **响应 `data`**：

| 字段 | 类型 | 说明 |
|------|------|------|

- **示例请求 / 响应**：（curl 或 JSON）

#### `POST /v1/...`

（同上结构。）

## 4. Webhook / 异步回调（如有）

| 事件 | 推送地址约定 | 重试策略 |
|------|----------------|----------|
| | | |

## 5. 变更记录

| 日期 | 版本 | 变更说明 |
|------|------|----------|
| | | |
