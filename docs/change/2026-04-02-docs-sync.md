# 迭代：文档与实现对齐（2026-04-02）

## 范围

- 更新 `docs/` 下 PRD、database-design、api、cache-design、deployment、development-guide、README、iteration-design，使之反映：
  - 多家庭：`households.code`（6 位数字）、Cookie 会话、中间件
  - 登录页：加入家庭 + **创建新家**（默认成员布布、一二）
  - 数据访问：仅 Service Role + Server Actions；**无 anon 业务读、无 Realtime**
  - 同步：**轮询**；localStorage 仅存家庭编码
  - 环境变量：以 `URL` + `SERVICE_ROLE_KEY` 为主；移除文档中的 `NEXT_PUBLIC_HOUSEHOLD_ID` 依赖描述

## 相关文件

- 根目录 [README.md](../../README.md)
- [../PRD.md](../PRD.md) 等主文档

## 备注

后续功能变更请同步更新对应 `docs/*.md`，并在本目录新增迭代文件或更新索引表。
