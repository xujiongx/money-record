# 迭代：SSGOI 路由页过渡、成员可点击提示、lucide 图标

## 迭代标识

- **名称 / 编号**：2026-09-03
- **时间**：2026-09-03
- **负责人**：xujiong

## 目标（对齐 PRD）

1. 按 [SSGOI × Next.js](https://ssgoi.dev/docs/frameworks/nextjs) 为移动端账本补上路由级页面过渡。
2. 优化成员页列表样式，让用户一眼看出可进入成员详情。
3. 二级页返回与常用控件改用 [lucide-react](https://lucide.dev/) 统一图标。

## 范围

| 类型 | 内容 |
|------|------|
| 包含 | SSGOI；成员列表可点击提示；`lucide-react`；`BackLink`；底栏/刷新/搜索/翻页图标 |
| 不包含 | 聊天吉祥物 SVG、记账分类装饰图标全量替换 |

## 技术设计摘要

### SSGOI / 壳层

- 页层实色底 + drill 嵌套；一级 Tab 点击 **`slide()`**（已去掉左右拖动手势切 Tab）
- **`lib/navigation/shell-tabs.ts`**：`isPrimaryTabPath`；仅一级 Tab 显示底栏与小布

### 成员列表

成员行「查看明细」+ 箭头提示

### lucide-react

- 依赖：`lucide-react`
- **`components/common/BackLink.tsx`**：`ChevronLeft` + 文案，供分析页成员明细、成员账单等二级页复用
- **`MobileShell`**：`Home` / `PenLine` / `BarChart3` / `Users`
- 其它：`ChevronLeft`/`ChevronRight`（统计翻页）、`RefreshCw`、`Search`/`X`（成员账单搜索）

## 任务拆分

| 任务 | 状态 |
|------|------|
| SSGOI 接入与透色修复 | ✅ |
| 成员列表可点击提示 | ✅ |
| 安装 lucide-react + BackLink | ✅ |
| 底栏与其它常用 icon | ✅ |
| 文档同步 | ✅ |

## 发布与验证

- [ ] 无左右拖动手势切 Tab
- [ ] 一级 Tab 点击为 slide 过渡
- [ ] 二级页（分析、成员账单等）无底栏
- [ ] 区间明细 / 成员账单使用 **fixed** **SubpageHeader**
- [ ] `/login` 进出正常

---

## 记账发生时间：快捷日 + 轻量滚轮列

### 目标

1. App 风 **年 / 月 / 日 / 时 / 分** 滚轮一体选择（非日历格子、非系统 `datetime-local`）。
2. 保留 **今天 / 昨天 / 前天**。

### 范围

| 类型 | 内容 |
|------|------|
| 包含 | `react-mobile-picker`；`OccurredAtPicker` 底部滚轮面板；快捷芯片 |
| 不包含 | antd-mobile、react-datepicker、react-day-picker |

### 技术设计摘要

- 依赖：**`react-mobile-picker`**（几乎无样式，五列受控滚轮）
- 交互：触发条 → 底部 sheet 草稿滚动 → **完成** 写回；点遮罩取消
- 日列随年月变化截断（如 2 月）；年份窗口为近 10 年～明年
- 快捷日仍直接改表单值（保留原时分）

### 任务

| 任务 | 状态 |
|------|------|
| 接入 react-mobile-picker 五列滚轮 | ✅ |
| 文档同步 | ✅ |

### 验证

- [ ] `/record`：快捷日；打开滚轮选年/月/日/时/分后完成回填
- [ ] 大月小月日列正确；编辑弹窗内可打开
- [ ] 滚轮滑动时二级页 **SubpageHeader** 不跟着拖（顶栏改为 viewport `fixed`）
- [ ] 二级顶栏深色字 + 浅实底可读
