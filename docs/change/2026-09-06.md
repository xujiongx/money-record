# 迭代：更多功能 / 大事记账

## 迭代标识

- **名称 / 编号**：tools-major-events
- **时间**：2026-09-06
- **负责人**：—

## 目标（对齐 PRD）

成员页提供「更多功能」入口；工具箱首发「大事记账」——按大项目统计支出、区分家庭成员（布布 / 一二），并提供分类与成员分析；数据与日常 `transactions` 完全隔离。

## 范围

| 类型 | 内容 |
|------|------|
| 包含 | `/tools` 工具箱；`major_events` / `major_event_expenses` 表；CRUD + 分析页；成员页入口；中间件 / SSGOI |
| 不包含 | 大事收入、编辑单笔支出、小布助手接入大事、归档 UI 细化 |

## 技术设计摘要

- 接口：`app/actions/major-events.ts`，缓存标签 `major-events`（不碰 `ledger`）
- 表结构：`004_major_events.sql`；复用 `members` 区分记账人
- 缓存：见 [cache-design.md](../cache-design.md)（独立标签）

## 任务拆分

| 任务 | 负责人 | 状态 |
|------|--------|------|
| 迁移与 Server Actions | — | ✅ |
| 工具箱 / 大事列表 / 明细 / 分析 UI | — | ✅ |
| 成员入口、中间件、SSGOI、文档 | — | ✅ |

## 发布与验证

- 在 Supabase 执行 `004_major_events.sql` 与 `005_major_event_categories.sql`
- 验证：成员 → 更多功能 → 大事记账 → 新建 → 记账（切换成员）→ 分析；确认首页/统计无新流水

## 回顾

- 完成情况：MVP 已落地
- 遗留问题：单笔支出编辑；归档状态 UI
- 下迭代输入：更多工具、大事预算额度
